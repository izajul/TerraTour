<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.monordevelopers.tt.terratour">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/terratour"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/NoactionBarTheme">
        <activity android:name=".activity.UserEntryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.UserSignupActivity">
        </activity>
        <activity
            android:name=".activity.UserProfileViewActivity"
            android:label="@string/title_activity_user_profile_view"
            android:theme="@style/NoactionBarTheme">
        </activity>
        <activity android:name=".activity.Event_Expense">
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity android:name=".activity.ShowNearByDetailsActivity">
        </activity>
        <activity android:name=".activity.NearbyLocationDetailsActivity">
        </activity>
        <activity android:name=".activity.WeatherActivity">
        </activity>
    </application>

</manifest>